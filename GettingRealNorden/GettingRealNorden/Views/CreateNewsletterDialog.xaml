<Window x:Class="GettingRealNorden.Views.CreateNewsletterDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:GettingRealNorden.Views"
        mc:Ignorable="d"
        WindowState="Maximized"
        WindowStartupLocation="CenterScreen"
        Title="CreateNewsletterDialog" Height="700" Width="1000">
    <Grid Background="White">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" MinHeight="50"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto" MinHeight="50"/>
        </Grid.RowDefinitions>
        <Grid Grid.Row="0" Background="#FF719AE7">
            <Label Content="Databind company name hertil"
                   Foreground="White"
                   FontSize="24"
                   FontWeight="ExtraBold"
                   FontStyle="Italic">
            </Label>
        </Grid>
        <Grid Grid.Row="2"  Background="#FF719AE7">
            <Label Content="Databind Dato og tid hertil"
                   Foreground="White"
                   FontSize="24"
                   FontWeight="ExtraBold"
                   FontStyle="Italic">
            </Label>
        </Grid>
        <Grid Grid.Row="1" Margin="20">
            <StackPanel>
                <TextBlock Text="Opret Nyhedsbrev"
                           FontSize="20"
                           FontWeight="Bold"
                           Margin="0,0,0,10"/>
                <TextBox x:Name="txtNewsletterTitle"
                         Text="{Binding Path=SelectedNewsletter.Title}"
                         Margin="0,0,0,10"
                         Cursor="IBeam"/>
                <TextBox x:Name="txtNewsletterContent"
                         Text="{Binding Path=SelectedNewsletter.News}"
                         MinHeight="200"
                         Height="350"
                         TextWrapping="Wrap"
                         Cursor="IBeam"
                         VerticalScrollBarVisibility="Auto"
                         Margin="0,0,0,10"/>
                <Button Content="Gem Nyhedsbrev"
                        Width="150"
                        Height="40"
                        Cursor="Hand"
                        Background="#FF4B4B9E"
                        Foreground="White"
                        FontWeight="Bold" />
            </StackPanel>

        </Grid>
    </Grid>

</Window>
